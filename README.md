For my emacs configuration. The configuration probably is for emacs 30.0 or later.

Suggestion for using configuration in this repo: **copy through manual typing**, so the configuration is understood.
